rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Profile-picture upload feature removed: deny all storage access.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
